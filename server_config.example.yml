# 监听地址
HOST: "::"
# 监听端口
PORT: 12346

# 是否启用 HTTP 服务
HTTP_SERVICE: false
# HTTP 服务监听端口
HTTP_PORT: 12347

# 单间最大用户数
ROOM_MAX_USERS: 8

# 观战用户ID
monitors:
  - 2

# 服务器名称（欢迎信息会使用）
server_name: Phira MP

# 管理员接口鉴权 token（HTTP /admin/*）
# ADMIN_TOKEN: "replace_me"

# 管理员数据持久化路径（JSON）
# ADMIN_DATA_PATH: "./admin_data.json"

